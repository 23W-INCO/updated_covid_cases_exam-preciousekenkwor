// .devcontainer/devcontainer.json
{
  "name": "UI Codespace",
  
  
  "dockerComposeFile": "docker-compose.yml",
  "service": "devcontainer",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",

  "remoteUser": "vscode",

  "forwardPorts": [8000],
  "postCreateCommand": "npm i && pip install -r requirements.txt",
  
  
 
}
